name: eventikets
description: Application mobile de gestion d'événements et de billetterie

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^1.1.0
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.2.4
  intl: ^0.20.2
  qr_flutter: ^4.1.0
  qr_code_scanner: ^1.0.1
  flutter_stripe: ^11.5.0
  flutter_local_notifications: ^19.1.0
  fl_chart: ^0.70.2
  flutter_animate: ^4.2.0
  
  # Firebase dependencies
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  firebase_storage: ^12.4.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/